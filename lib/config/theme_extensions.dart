/// Barrel for the Kiduna design-token [ThemeExtension]s and motion constants.
///
/// All Kiduna Field tokens — colours, typography, layout metrics, shadows, and
/// motion — are registered on the app [ThemeData] and read through these
/// extensions. Import this file to access any of them; see
/// `context_extensions.dart` for the `context.kiduna`, `context.kidunaText`,
/// `context.metrics`, and `context.shadows` helpers.
library;

export 'kiduna_colors.dart';
export 'kiduna_metrics.dart';
export 'kiduna_motion.dart';
export 'kiduna_shadows.dart';
export 'kiduna_text.dart';
